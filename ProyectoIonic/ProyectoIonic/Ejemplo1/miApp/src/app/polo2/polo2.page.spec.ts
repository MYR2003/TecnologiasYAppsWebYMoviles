import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Polo2Page } from './polo2.page';

describe('Polo2Page', () => {
  let component: Polo2Page;
  let fixture: ComponentFixture<Polo2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Polo2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
