import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Polo6Page } from './polo6.page';

describe('Polo6Page', () => {
  let component: Polo6Page;
  let fixture: ComponentFixture<Polo6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Polo6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
