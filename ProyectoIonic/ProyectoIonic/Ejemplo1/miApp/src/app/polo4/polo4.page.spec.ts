import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Polo4Page } from './polo4.page';

describe('Polo4Page', () => {
  let component: Polo4Page;
  let fixture: ComponentFixture<Polo4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Polo4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
