import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Polo3Page } from './polo3.page';

describe('Polo3Page', () => {
  let component: Polo3Page;
  let fixture: ComponentFixture<Polo3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Polo3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
